package templates;

import src.F;
import src.P;

public class Cons {
    // the item inside this list node
    int v;
    // The next element, null if nothing
    Cons next;
    // Constructor
    Cons(int v, Cons next) {
        this.v = v;
        this.next = next;
    }
@   @student_generic@@    
}