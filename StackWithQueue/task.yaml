accessible: true
author: Yakoub J, Rucquoy A
categories:
- module3
context: |-
    In this short exercise we will ask you to implement a LIFO stack using a queue (provided as a Java ``LinkedList<E>``). We need you to provide 4 methods :
    ``push``, ``pop``, ``peek``, ``empty``. You can't use the ``LinkedList`` API expect for the methods listed below:

    - ``add``
    - ``remove``
    - ``peek``
    - ``isEmpty``

    (You can find the file here_).

    .. _here: /course/LEPL1402/StackWithQueue/MyStack.java

    .. code-block:: java

        import java.util.LinkedList;

        public class MyStack<E> {

            private LinkedList<E> queue;

            /*
            * Constructor
            */
            public MyStack() {
                this.queue = new LinkedList<>();
            }

            /*
            * push an element at top (remember, a stack is "Last In First Out")
            */
            public void push(E elem) {
                //TODO
            }

            /*
            * Return the top of the stack AND remove the retrieved element
            */
            public E pop() {
                //TODO
            }

            /*
            * Return the top element of the stack, without removing it
            */
            public E peek() {
                //TODO
            }

            /*
            * Tells if the stack is empty or not
            */
            public boolean empty() {
                //TODO
            }
environment: java8scala
evaluate: best
file: ''
groups: false
input_random: '0'
limits:
    memory: '100'
    time: '30'
    output: '2'
name: '[Module 3] Stack with a Queue'
network_grading: false
order: 25
problems:
    student_add:
        language: java
        header: Paste here the **body** of your method ``push()``
        name: ''
        default: ''
        type: code
    student_remove:
        header: Paste here the **body** of your method ``pop()``
        type: code
        default: ''
        language: java
        name: ''
    student_peek:
        default: ''
        type: code
        name: ''
        header: Paste here the **body** of your method ``peek()``
        language: java
    student_isEmpty:
        language: java
        default: ''
        header: Paste here the **body** of your method ``empty()``
        type: code
        name: ''
stored_submissions: 0
submission_limit:
    amount: -1
    period: -1
weight: 1.0
