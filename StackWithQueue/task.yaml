accessible: true
author: Yakoub J, Rucquoy A
categories:
- module2
context: |-
    In this short exercise we will ask you to implement a LIFO stack using a queue (provided as a Java ``LinkedList<E>``). We need you to provide 4 methods :
    ``push``, ``pop``, ``peek``, ``empty``. You can't use the ``LinkedList`` API except for the methods listed below:

    - ``add``
    - ``remove``
    - ``peek``
    - ``isEmpty``
    - ``size``

    The challenge of this exercise is to use **only** the queue that is provided as instance variable: ``queue``. In other words, you **can't** instantiate a new ``LinkedList`` anywhere. Here is the skeleton of the method

    .. code-block:: java

        import java.util.LinkedList;

        public class MyStack<E> {

            private LinkedList<E> queue;

            /*
             * Constructor
             */
            public MyStack() {
                this.queue = new LinkedList<>();
            }

            /**
             * Push an element on the stack
             *
             * @param elem the Element to push
             */
            public void push(E elem) {
            }

            /**
             * Remove the element at the top of the stack
             *
             * @return The element at the top of the stack
             */
            public E pop() {
                return null;
            }

            /**
             * Look at the element at the top of the stack
             *
             * @return The element at the top of the stack
             */
            public E peek() {
                return null;
            }

            /**
             * Is the stack empty
             *
             * @return True if there is no element in the stack
             *         and false otherwise
             */
            public boolean empty() {
                return false;
            }

        }
environment: java8scala
evaluate: best
file: ''
groups: false
input_random: '0'
limits:
    memory: '100'
    time: '30'
    output: '2'
name: '[Module 2] Stack with a Queue'
network_grading: false
order: 25
problems:
    student_add:
        name: ''
        header: Paste here the **body** of your method ``push()``
        type: code
        default: ''
        language: java
    student_remove:
        language: java
        name: ''
        header: Paste here the **body** of your method ``pop()``
        type: code
        default: ''
    student_peek:
        name: ''
        header: Paste here the **body** of your method ``peek()``
        type: code
        language: java
        default: ''
    student_isEmpty:
        default: ''
        type: code
        name: ''
        header: Paste here the **body** of your method ``empty()``
        language: java
run_cmd: ''
stored_submissions: 0
submission_limit:
    amount: -1
    period: -1
weight: 1.0
